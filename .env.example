BROWSER_HEADLESS=false
BROWSER_SLOW_MO_MS=150
SCREENSHOT_DIR=screenshots

# Optional defaults used when no CLI args are passed.
# Point at a task file (declarative, recommended):
TASK_FILE=tasks/shadcn.json
# ...or just a URL for zero-config auto-fill of every field:
# TARGET_URL=https://httpbin.org/forms/post
